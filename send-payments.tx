send 0.01 eth to vitalik.eth on arbitrum;
send 10 usdc on ethereum to ian.eth;

stmt -> value
      | target
      | chain

value -> send amount unit

target -> to address | ens

chain -> on ethereum
       | arb
       | op
       | bnb
       | poly

// Primitives
amount -> 0 | .. | 1 | amount

unit -> eth | wei | gwei

address -> 

ens -> 
